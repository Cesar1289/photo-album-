# Photo Album

## Run in Replit Preview

The project is a React app powered by Vite. Its Replit workflow runs:

```bash
npm run dev
```

The development server listens on port `3000` and is available through Replit Preview.

For a production-style local check, run:

```bash
npm run build
npm run preview
```